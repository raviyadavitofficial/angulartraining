import { SecondCustomPipePipe } from './second-custom-pipe.pipe';

describe('SecondCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
